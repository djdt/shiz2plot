import re


class ChromFilter(object):
    def __init__(self, *args, **kwargs):

        for key, value in kwargs.items():
            if key == 'event':
            elif key == 'channel':
            elif key == 'precursor':
            elif key == 'product':
            elif key == 'file':

